select *from books;
select category,price_incl_tax
select category,price_excl_tax
select availability,stars
select category,count(*) as  total_books,
select id ,title ,num_reviews,stars,category
select category ,avg(stars) as average_stars
select title,price_incl_tax,
select  b.category, b.title, b.price_incl_tax
SELECT category,
SELECT title, category, price_incl_tax
SELECT MAX(price_incl_tax)
SELECT title, price_incl_tax,